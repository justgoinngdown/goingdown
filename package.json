{
  "name": "quality-inspection-website",
  "version": "1.0.0",
  "scripts": {
    "dev": "tinacms dev -c \"npx http-server . --port 3000\"",
    "build": "tinacms build",
    "start": "npx http-server . --port 3000"
  },
  "devDependencies": {
    "@types/node": "^22.13.1"
  },
  "dependencies": {
    "@tinacms/auth": "^1.0.3",
    "@tinacms/cli": "^1.5.29",
    "tinacms": "^1.5.20",
    "http-server": "^14.1.1"
  }
}
